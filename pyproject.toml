[project]
name = "financeflow"
version = "0.4.0"
requires-python = ">=3.14"
dependencies = [
    "colorlog>=6.11.0",
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "pyfakefs>=6.2.0",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]
